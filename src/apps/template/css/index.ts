import { IRawSubject } from '../../../modules/shared/types.js';

const Subject = (): IRawSubject => ({
  name: 'CSS',
  topics: [
    'text-decoration',
    'text-align',
    'visibility',
  ],
});

export default Subject;
