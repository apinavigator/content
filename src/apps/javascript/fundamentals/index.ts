import { IRawSubject } from '../../../shared/types.js';

const Subject = (): IRawSubject => ({
  name: 'Fundamentals',
  topics: [
    'data-types-and-structures',
    'length',
  ],
});

export default Subject;
